import numpy as np
# X = np.random.normal(1,10,(1000,50))
# print(X)
# m = np.mean(X, axis=0)
# std = np.std(X, axis=0)
# X_norm = ((X - m)  / std)
# print (X_norm)
x = np.random.normal( 1,10,(1,1) )
print ( x )